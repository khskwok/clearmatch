{
  "name": "clearmatch-api",
  "version": "1.0.0",
  "description": "ClearMatch MPF Reconciliation Assistant API",
  "main": "index.js",
  "scripts": {
    "start": "func start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@azure/functions": "^4.5.0"
  }
}
